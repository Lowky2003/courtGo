# Brainstorming visual companion (local mockups, not part of the app)
.superpowers/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Environment / secrets (never commit Stripe keys etc.)
.env
.env.*
!.env.example

# Laravel (added properly when the app is scaffolded)
/vendor/
/node_modules/
/public/build/
/public/hot
/storage/*.key
